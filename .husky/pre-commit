#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Same gate agents/Lovable must run before commit (see AGENTS.md).
# Autofix first, re-stage fixed paths that were already staged, then verify.
QUALITY_GATE_RESTAGE=1 sh scripts/quality-gate.sh --fix-and-check
